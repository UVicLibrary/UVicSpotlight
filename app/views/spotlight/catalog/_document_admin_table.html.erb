<%# container for all documents in index view -%>
<table id="documents" class="table">
  <thead>
    <tr>
      <th class="col-sm-1 thumbnail-column"></th>
      <th scope="col" class="col-sm-3"><%= t(:'spotlight.catalog.fields.title') %></th>
      <th scope="col">Item ID</th>
      <th scope="col" class="col-sm-2">File Type</th>
      <th scope="col" class="col-sm-2 text-nowrap"><%= t(:'spotlight.catalog.fields.date_added') %></th>
      <th scope="col" class="col-sm-1 checkbox-toggle"><%= t(:'spotlight.catalog.fields.visibility') %></th>
      <th class="delete-button-column"></th>
    </tr>
  </thead>
  <!-- Render each document as a row -->
  <%= render partial: 'document_row', collection: documents, as: :document %>
</table>
