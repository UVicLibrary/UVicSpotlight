<% if featured_pages_block.pages? %>
  <div class="spotlight-flexbox browse-categories categories-<%= featured_pages_block.pages.length %>" data-sidebar='<%= @page.display_sidebar? %>'>
    <% featured_pages_block.pages.each_with_index do |page, index| %>
      <div class="box category-<%= (index + 1) %>">
        <%= link_to [spotlight, current_exhibit, page] do %>
          <div class="browse-category" style='background-image: linear-gradient(rgba(0, 0, 0, 0.0), rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.8)) <%= ", url(\"#{page.thumbnail_image_url}\")" if page.thumbnail_image_url %><%= "; width:104%" if featured_pages_block.pages.length == 2 %>'>
            <div class="category-caption">
              <p class="category-title">
                <%= page.title %>
              </p>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
<% end %>
